import { LightningElement } from 'lwc';

export default class CustomButtonCommunity extends LightningElement {
    handleClick() {
        // Button का logic यहाँ लिखो
        alert('Button clicked!');
    }
}